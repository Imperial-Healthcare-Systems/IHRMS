# IHRMS Audit

Auto-generated by the multi-agent audit workflow.

**Confirmed findings:** 65
(Verified by 3 adversarial reviewers per finding; majority-real required to survive.)

## Summary by dimension

| Dimension | Surveyed | Confirmed | Rejected |
|---|---:|---:|---:|
| Auth + RLS boundary | 15 | 10 | 5 |
| Schema-code drift | 16 | 14 | 2 |
| Route correctness | 15 | 13 | 2 |
| Business logic | 15 | 12 | 3 |
| Performance | 12 | 6 | 6 |
| Observability | 10 | 10 | 0 |

## Summary by severity

- **Critical:** 6
- **High:** 30
- **Medium:** 20
- **Low:** 9

---

## Critical (6)

### 1. Identity/Employee ID Confusion in Multiple Routes

- **File:** `app/api/attendance/route.ts:85`
- **Severity:** critical
- **Dimension:** Auth + RLS boundary
- **Category:** Identity Mismatch

**What's wrong**

Routes are using ctx.identityId (identities.id) directly in .eq('employee_id', ctx.identityId) clauses. However, ctx.identityId is identities.id, not employees.id. These queries will fail to match employee records unless the identity happens to have a coincidental match.

**Evidence**

```
.eq('employee_id', ctx.identityId) in GET when user is not FULL_ACCESS_ROLES. ctx.identityId is documented as identities.id in lib/session.ts line 29, but code assumes it matches employees.id.
```

**Impact**

Non-admin users querying their own attendance, leaves, payslips, expense claims, documents, training records, etc. will see empty results. In reimbursements/route.ts line 63, a manager cannot view their team's claims. Users cannot view their own data across multiple modules.

**Suggested fix**

Must query employees table first to resolve identities.id → employees.id mapping, or use RLS policies to enforce org/employee scoping instead of app-level filters.

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 2. Reimbursement Query Uses ctx.identityId to Filter expense_claims.employee_id

- **File:** `app/api/reimbursements/route.ts:63`
- **Severity:** critical
- **Dimension:** Auth + RLS boundary
- **Category:** Identity Mismatch

**What's wrong**

Line 63 uses .eq('employee_id', ctx.identityId) for non-admin users. Since ctx.identityId is identities.id and employees.employee_id expects employees.id, the query returns no results. Also, line 55 queries employees.manager_id against ctx.identityId without resolving the identity first.

**Evidence**

```
Line 63: query = query.eq('employee_id', ctx.identityId). Line 55: .eq('manager_id', manager_id) but manager_id comes from URL and is compared as string without lookup.
```

**Impact**

Non-admin users and managers cannot view any reimbursement claims. Line 55 manager team lookup will also fail because ctx.identityId (identity) is compared against employees.manager_id (expects employee.id).

**Suggested fix**

Must first look up employees.id from identities.id before filtering claims. Or use identity_id foreign key in employees table for direct matching.

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 3. Exit/Warnings Routes Filter By ctx.identityId Against employee_id

- **File:** `app/api/exit/route.ts:60`
- **Severity:** critical
- **Dimension:** Auth + RLS boundary
- **Category:** Identity Mismatch

**What's wrong**

The exit and warnings routes use .eq('employee_id', ctx.identityId) for non-admin filtering. This same identity mismatch affects employee's ability to view their own exit and warning records.

**Evidence**

```
Line 60 (exit): .eq('employee_id', ctx.identityId). Similar pattern in warnings/route.ts line 54. ctx.identityId is identities.id, not employees.id.
```

**Impact**

Employees cannot view their own exit clearance status or warning records. A critical self-service feature is broken for all non-admin users.

**Suggested fix**

Implement identity → employee_id resolution in lib/session.ts or create a stored lookup table.

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 4. ctx.identityId used as employees.id without validation

- **File:** `c:/Users/ROSE MEHRA/Downloads/IHRMS-2/app/api/attendance/route.ts:116`
- **Severity:** critical
- **Dimension:** Business logic
- **Category:** Identity Mapping

**What's wrong**

Code assumes ctx.identityId equals employees.id, but per tenant-spec: identities.id → employees.identity_id; employees.id is a distinct UUID. When employee_id parameter is omitted, code uses ctx.identityId as targetEmployee, which may not exist in employees.id column.

**Evidence**

```
const targetEmployee = employee_id ?? ctx.identityId
.eq('employee_id', targetEmployee)
```

**Impact**

Punch-in/punch-out will silently fail or create ghost records with wrong employee_id. Attendance rolls up incorrectly. This pattern repeats across leaves/reimbursements.

**Suggested fix**

Look up employees.id via employees.identity_id = ctx.identityId before using as targetEmployee

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 5. ctx.identityId used as employees.id in leave requests

- **File:** `c:/Users/ROSE MEHRA/Downloads/IHRMS-2/app/api/leaves/route.ts:107`
- **Severity:** critical
- **Dimension:** Business logic
- **Category:** Identity Mapping

**What's wrong**

Same identity_id vs employee_id mismatch in leave creation. targetEmployee defaults to ctx.identityId but must be employees.id.

**Evidence**

```
const targetEmployee = employee_id ?? ctx.identityId
insert({ employee_id: targetEmployee, ... })
```

**Impact**

Leave requests created with wrong employee_id, orphaning them from the actual employee record. Leave balance deductions fail silently.

**Suggested fix**

Resolve ctx.identityId to employees.id before inserting leave_requests

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 6. Filtering on non-existent 'org_id' column across 79 route files

- **File:** `app/api/designations/route.ts:26`
- **Severity:** critical
- **Dimension:** Schema-code drift
- **Category:** Missing Column

**What's wrong**

Codebase contains 287 instances of .eq('org_id', ctx.orgId) across 79 files, but supabase-schema.sql defines zero org_id columns. Multi-tenant filtering will silently fail.

**Evidence**

```
.eq('org_id', ctx.orgId) on designations, warning_letters, employees, attendance_*, payslips, etc.
```

**Impact**

Multi-tenant isolation fails silently. Queries return empty results because org_id column doesn't exist. Cross-tenant data leaks possible.

**Suggested fix**

Add org_id NOT NULL column to all tables; backfill with tenant context; update schema DDL

<sub>Verifier consensus: 3/3 real, confidence high</sub>

---

## High (30)

### 7. Payroll Routes Use requireAuth() Instead of Stronger Role Gating

- **File:** `app/api/payroll/[id]/route.ts:21`
- **Severity:** high
- **Dimension:** Auth + RLS boundary
- **Category:** Insufficient Role Gating

**What's wrong**

GET /api/payroll/[id] uses requireAuth() which allows ANY authenticated user to read sensitive payroll run details (gross_salary, net_pay, deductions, payslip counts). Only PATCH properly gates with requireRole(PAYROLL_ROLES).

**Evidence**

```
GET handler line 21: requireAuth() with no role check. PATCH handler line 63: requireRole(PAYROLL_ROLES) with correct role list ['owner', 'admin', 'hr_admin', 'super_admin', 'hr', 'payroll_admin', 'finance_admin'].
```

**Impact**

Any employee can call GET /api/payroll/[id] and read aggregate salary statistics, total gross/net for an entire run, and payslip counts. Sensitive financial data exposed beyond payroll administrators.

**Suggested fix**

Change line 21 to: const { ctx, error } = await requireRole(PAYROLL_ROLES)

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 8. Salary Structures Endpoint Allows All Authenticated Users to Read Payroll Data

- **File:** `app/api/payroll/salary-structures/route.ts:18`
- **Severity:** high
- **Dimension:** Auth + RLS boundary
- **Category:** Insufficient Role Gating

**What's wrong**

GET /api/payroll/salary-structures uses requireAuth() (no role gate) and returns all salary structures in the org with optional employee_id filter. Any employee can read any other employee's salary information.

**Evidence**

```
Line 18: const { ctx, error } = await requireAuth() - only session auth, no role check. Returns full salary rows including ctc_annual, basic, hra, special_allowance, etc.
```

**Impact**

Salary information is sensitive and must be restricted to HR/payroll roles. Current implementation allows any employee to enumerate all salary structures or query a specific employee's compensation.

**Suggested fix**

Add requireRole(['owner', 'admin', 'hr_admin', 'super_admin', 'hr', 'payroll_admin', 'finance_admin']) at line 18.

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 9. Onboarding Route Uses requireAuth() Instead of requireRole() for GET

- **File:** `app/api/onboarding/route.ts:11`
- **Severity:** high
- **Dimension:** Auth + RLS boundary
- **Category:** Insufficient Role Gating

**What's wrong**

GET /api/onboarding uses requireAuth() (line 11) with no role check, allowing any employee to list all employees in probation status along with their document counts and onboarding completion status.

**Evidence**

```
Line 11: const auth = await requireAuth(). POST correctly uses requireRole(HR_ROLES) at line 105.
```

**Impact**

Employees can retrieve onboarding status and document counts for all new hires in the org. This is an information disclosure vulnerability for sensitive HR data.

**Suggested fix**

Change line 11 to: const auth = await requireRole(HR_ROLES)

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 10. Leaves Route Uses ctx.identityId to Filter By Reporting Manager ID

- **File:** `app/api/leaves/route.ts:54`
- **Severity:** high
- **Dimension:** Auth + RLS boundary
- **Category:** Identity Mismatch

**What's wrong**

Line 54 queries .eq('reporting_manager_id', ctx.identityId). The reporting_manager_id field stores employees.id, but ctx.identityId is identities.id. A manager cannot filter their direct reports.

**Evidence**

```
Line 50-54: For MANAGER_ROLES, queries employees.reporting_manager_id against ctx.identityId. Should lookup employees.id first.
```

**Impact**

Managers cannot view leave requests from their team using the manager view. The team member list will be empty because ctx.identityId (identity) never matches any employees.reporting_manager_id values.

**Suggested fix**

Lookup the manager's employees.id from ctx.identityId before filtering direct reports.

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 11. Ecosystem Consume Endpoint Missing Org Filtering on Notification Insert

- **File:** `app/api/ecosystem/consume/route.ts:29`
- **Severity:** high
- **Dimension:** Auth + RLS boundary
- **Category:** Cross-Tenant Leakage

**What's wrong**

When inserting notifications for CRM events (line 29), the endpoint accepts employee_id from the payload without verifying it belongs to org_id. An external service could insert notifications for an employee in a different org.

**Evidence**

```
Line 26: const { employee_id, deal_value, deal_name } = payload ?? {}. Line 29-35: supabaseAdmin.from('notifications').insert({ recipient_id: employee_id, ... }). No check that employee_id belongs to org_id.
```

**Impact**

A compromised or malicious ICRM instance can insert notifications for any employee UUID in the system, potentially targeting users in other tenants.

**Suggested fix**

Verify employee_id exists in requested org_id before insert: .select('id').eq('id', employee_id).eq('org_id', org_id).maybeSingle()

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 12. Documents Route Uses identity_id to Filter org_documents Without Validation

- **File:** `app/api/documents/route.ts:44`
- **Severity:** high
- **Dimension:** Auth + RLS boundary
- **Category:** Identity Mismatch

**What's wrong**

Line 44 filters .eq('employee_id', ctx.identityId) for non-admin users. The documents table may have an employee_id foreign key to employees.id, not identities.id. This query will return empty results for all non-admin users.

**Evidence**

```
Line 44: if (!isAdminUser) q = q.eq('employee_id', ctx.identityId). ctx.identityId is identities.id per session.ts, but employee_id likely expects employees.id.
```

**Impact**

Non-admin employees cannot access their own uploaded documents or employee-specific documents. The document retrieval feature is broken.

**Suggested fix**

Query employees table first to get employees.id from ctx.identityId, then filter documents.

<sub>Verifier consensus: 2/3 real, confidence high</sub>

### 13. Reimbursement amount parsed with parseFloat() — loses precision on INR amounts

- **File:** `c:/Users/ROSE MEHRA/Downloads/IHRMS-2/app/api/reimbursements/route.ts:150`
- **Severity:** high
- **Dimension:** Business logic
- **Category:** Money Math

**What's wrong**

INR amounts stored as floating-point using parseFloat(amount). JavaScript Number does not preserve decimal precision beyond ~15 significant digits. For multi-claim reconciliation or statutory reporting, 0.01 INR rounding errors compound.

**Evidence**

```
amount: parseFloat(amount)
```

**Impact**

Monthly reimbursement reports will not reconcile exactly with payroll deductions. Audit trails show discrepancies. Statutory compliance ledgers mismatch.

**Suggested fix**

Store amount as INTEGER (paise) or use a decimal library; validate precision in schema

<sub>Verifier consensus: 2/3 real, confidence high</sub>

### 14. No validation that employee is not on leave when marking present

- **File:** `c:/Users/ROSE MEHRA/Downloads/IHRMS-2/app/api/attendance/route.ts:137`
- **Severity:** high
- **Dimension:** Business logic
- **Category:** Cross-Row Invariants

**What's wrong**

punch_in action creates/updates attendance_daily with status='present'/'late'/'work_from_home' without checking leave_requests table. An employee can be marked present AND have an approved leave on the same date, violating business logic.

**Evidence**

```
// ── PUNCH IN ─────────────────────
if (action === 'punch_in') {
  // No query to leave_requests for date overlap
```

**Impact**

Attendance reports show duplicate records (both present and on_leave for same date). Payroll calculates LOP and salary deduction simultaneously, double-charging the employee.

**Suggested fix**

Before inserting/updating attendance_daily, query leave_requests for overlapping approved leave and reject or warn

<sub>Verifier consensus: 2/3 real, confidence high</sub>

### 15. Expense claim inclusion in payroll doesn't validate prior approval

- **File:** `c:/Users/ROSE MEHRA/Downloads/IHRMS-2/app/api/reimbursements/[id]/route.ts:165`
- **Severity:** high
- **Dimension:** Business logic
- **Category:** State Transitions

**What's wrong**

include_payroll action checks current.status === 'approved', but no subsequent re-check before payroll deduction. A concurrent rejection could flip status between validation and payroll update, causing phantom deductions.

**Evidence**

```
if (action === 'include_payroll') {
  if (current.status !== 'approved') { return error }
  updatePayload.status = 'paid'
} // No re-fetch of current before update in payroll
```

**Impact**

A rejected claim still appears in payroll as deduction. Employee paid less than entitled. Statutory compliance audit fails.

**Suggested fix**

Re-fetch claim status inside payroll transaction or use a WHERE clause with status check

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 16. Leave balance decrement on approve is best-effort, not transactional

- **File:** `c:/Users/ROSE MEHRA/Downloads/IHRMS-2/app/api/leaves/[id]/route.ts:154`
- **Severity:** high
- **Dimension:** Business logic
- **Category:** Business Logic

**What's wrong**

After approving a leave request, code decrements leave_balance in a try-catch block marked 'best-effort'. If balance decrement fails, leave is already approved and employee balance stays positive, allowing double-usage.

**Evidence**

```
try { ... update leave_balances ... } catch (balErr) { console.warn('[leaves approve] balance update non-fatal:', balErr) }
```

**Impact**

Employee can request and approve multiple leaves totaling more than their balance. Payroll calculates negative LOP, underpaying employee or creating GL mismatch.

**Suggested fix**

Wrap approve + deduct in a Postgres transaction; rollback both if either fails

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 17. Audit log writes use console.warn instead of console.error on insert failure

- **File:** `c:/Users/ROSE MEHRA/Downloads/IHRMS-2/lib/audit.ts:71`
- **Severity:** high
- **Dimension:** Observability
- **Category:** Error handling in critical audit paths

**What's wrong**

The logAudit() fire-and-forget function uses console.warn() for audit_logs insert failures, including the fallback retry. Since logAudit() is used across the application to record state changes, insert failures should be console.error(). This makes critical data loss events harder to spot in monitoring.

**Evidence**

```
Line 71: `if (e2) console.warn('[audit.log fallback]', e2.message)` and Line 75: `console.warn('[audit.log]', error.message)`
```

**Impact**

Audit insert failures are logged at WARN severity instead of ERROR, so SRE alerting on ERROR-level logs will miss data integrity issues. The canonical audit trail becomes unreliable without operator visibility.

**Suggested fix**

Change console.warn to console.error in lib/audit.ts lines 71 and 75.

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 18. Impersonation audit insert does not block the login if tenant_visible_audit fails

- **File:** `c:/Users/ROSE MEHRA/Downloads/IHRMS-2/app/api/auth/impersonation-login/route.ts:265`
- **Severity:** high
- **Dimension:** Observability
- **Category:** Silent failure in customer-visible audit

**What's wrong**

When an Imperial admin impersonates a tenant user, the route inserts a row into tenant_visible_audit (§8.3) so the customer can audit the event. If this insert fails, the error is logged but the login completes anyway. The customer sees no record that they were impersonated.

**Evidence**

```
Line 265-268: `if (auditErr) { console.error('[impersonation-login] tenant_visible_audit INSERT FAILED:', ...)} // login continues`
```

**Impact**

Customers cannot audit who impersonated them because the audit row may silently fail to insert. This violates IMPERIAL_TENANT_SPEC §8.3 customer audit requirements and hides security events.

**Suggested fix**

Return a 500 error if auditErr is non-null; do not redirect if the audit insert fails.

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 19. Impersonation-end audit insert does not block the logout if tenant_visible_audit fails

- **File:** `c:/Users/ROSE MEHRA/Downloads/IHRMS-2/app/api/auth/impersonation-end/route.ts:55`
- **Severity:** high
- **Dimension:** Observability
- **Category:** Silent failure in customer-visible audit

**What's wrong**

When an impersonation ends, the route inserts a row into tenant_visible_audit so the customer can see when the impersonation ended. If this insert fails, the error is logged but the logout completes anyway. The customer has no record of the impersonation end time.

**Evidence**

```
Line 55: `if (error) console.error('[impersonation-end] tenant_visible_audit INSERT FAILED:', error.message, { org_id: orgId, log_id: logId })`
```

**Impact**

Customers cannot see when an impersonation ended, breaking the audit trail and violating IMPERIAL_TENANT_SPEC §8.3 visibility requirements.

**Suggested fix**

Log the error but consider whether a silent failure is acceptable. If not, return an error response instead of continuing to signOut.

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 20. Sensitive SMTP_USER email exposed in customer-facing welcome email HTML

- **File:** `c:/Users/ROSE MEHRA/Downloads/IHRMS-2/lib/mailer.ts:256`
- **Severity:** high
- **Dimension:** Observability
- **Category:** Sensitive data leakage

**What's wrong**

In sendWelcomeEmail(), the template embeds process.env.SMTP_USER directly into the HTML email sent to new employees. This leaks the support email address into the HTML of every welcome email and makes it visible in customer email headers/logs.

**Evidence**

```
Line 256: `For any queries, reach out to HR at <a href="mailto:${process.env.SMTP_USER}" style="color:#E8622A">${process.env.SMTP_USER}</a>`
```

**Impact**

The SMTP_USER (support/noreply email) is embedded in every welcome email sent to employees, leaking it to email logs, archival systems, and potentially external email security providers. An attacker could use this to craft targeted phishing.

**Suggested fix**

Use a branding-aware email address from org_branding.email_from_addr or a static 'support@' address instead of SMTP_USER.

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 21. N+1 Query: Team fetch in attendance regularization PATCH approval path

- **File:** `c/Users/ROSE MEHRA/Downloads/IHRMS-2/app/api/attendance/regularization/route.ts:156`
- **Severity:** high
- **Dimension:** Performance
- **Category:** N+1 Queries

**What's wrong**

In the PATCH handler, when a non-HR user (manager) approves/rejects a regularization request, the code fetches the employee's reporting_manager_id separately. However, during GET (line 54-62), managers already fetch their entire team list for filter purposes. The PATCH should reuse or cache this relationship rather than making a separate query.

**Evidence**

```
Line 157-162: `const { data: emp } = await supabaseAdmin.from('employees').select('reporting_manager_id').eq('id', (existing as any).employee_id).eq('org_id', ctx.orgId).single()` - This is one additional query per PATCH request that could be batched or cached.
```

**Impact**

On systems with many managers approving regularizations, each approval incurs an extra database roundtrip to fetch employee→manager relationship that may already be known.

**Suggested fix**

Cache the employee's reporting_manager_id earlier or include it in the existing.fetch query on line 147-152.

<sub>Verifier consensus: 2/3 real, confidence high</sub>

### 22. Missing composite index on org_id + status for multi-tenant queries

- **File:** `c/Users/ROSE MEHRA/Downloads/IHRMS-2/supabase-schema.sql`
- **Severity:** high
- **Dimension:** Performance
- **Category:** Missing Indexes

**What's wrong**

The schema has NO index on org_id column at all. Every tenant-scoped query (.eq('org_id', ctx.orgId)) must perform a full table scan on tables like attendance_daily, attendance_regularizations, leave_requests, payroll_runs, etc. This is critical for multi-tenant performance as the org_id filter is the primary tenant boundary.

**Evidence**

```
Grep shows zero 'CREATE INDEX ... org_id' statements in supabase-schema.sql. Code shows org_id is used in 50+ API routes (.eq('org_id', ctx.orgId)) but no corresponding indexes exist.
```

**Impact**

Severe performance degradation on large orgs. A query like .from('attendance_daily').eq('org_id', ctx.orgId).limit(100) on a shared database with millions of rows must scan all rows before the limit applies.

**Suggested fix**

Create composite indexes: CREATE INDEX idx_attendance_daily_org_id ON attendance_daily(org_id, attendance_date); CREATE INDEX idx_leave_requests_org_id ON leave_requests(org_id, status); etc. for all multi-tenant tables.

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 23. Fire-and-forget promise in announcements notification fanout

- **File:** `app/api/announcements/route.ts:50`
- **Severity:** high
- **Dimension:** Route correctness
- **Category:** Unhandled Promise Rejections

**What's wrong**

The supabaseAdmin.from('notifications').insert(notifications) call is awaited but the result is not destructured to check for errors. If the insert fails, the error is silently swallowed and the function continues, potentially leaving notifications unsent.

**Evidence**

```
await supabaseAdmin.from('notifications').insert(notifications)

No error checking: the line awaits but doesn't check `{ error }` destructure.
```

**Impact**

Users won't receive critical notifications (announcements) if the database insert fails. The function returns success despite partial/complete failure.

**Suggested fix**

const { error: notifErr } = await supabaseAdmin.from('notifications').insert(notifications); if (notifErr) { console.error('[announcements fanout]', notifErr); /* handle or rethrow */ }

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 24. Unvalidated async insert in notifications PATCH without error checking

- **File:** `app/api/notifications/route.ts:53`
- **Severity:** high
- **Dimension:** Route correctness
- **Category:** Silently-Swallowed PostgREST Errors

**What's wrong**

The PATCH handler calls supabaseAdmin.from('notifications').update() without capturing or checking the { error } result. If the update fails, the route still returns { ok: true } with status 200, misleading the client.

**Evidence**

```
if (body.markAllRead) {
      await supabaseAdmin
        .from('notifications')
        .update({ is_read: true })
        .eq('org_id', ctx.orgId)
        .eq('recipient_id', ctx.identityId)
        .eq('is_read', false)
    } else if (body.id) {
      await supabaseAdmin
        .from('notifications')
        .update({ is_read: true })
        .eq('id', body.id)
        .eq('org_id', ctx.orgId)
        .eq('recipient_id', ctx.identityId)
    }

    return NextResponse.json({ ok: true })
```

**Impact**

Notifications remain unread in the database even though the client is told the update succeeded (status 200). This creates a data integrity issue where UI state diverges from DB state.

**Suggested fix**

const { error: updateErr } = await supabaseAdmin.from('notifications').update(...); if (updateErr) { console.error('[notifications PATCH]', updateErr); return NextResponse.json({ error: updateErr.message }, { status: 500 }); }

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 25. Fire-and-forget ecosystem event processing with void Promise

- **File:** `app/api/ecosystem/consume/route.ts:28`
- **Severity:** high
- **Dimension:** Route correctness
- **Category:** Unhandled Promise Rejections

**What's wrong**

The ecosystem/consume endpoint uses `void Promise.resolve(supabaseAdmin.from(...).insert(...)).catch(() => {})` to fire-and-forget notifications. If the insert fails after the catch, it's silently swallowed; if the catch itself rejects, it cascades. Additionally, this anti-pattern (void + Promise.resolve) makes it unclear that errors are being ignored.

**Evidence**

```
void Promise.resolve(
    supabaseAdmin.from('notifications').insert({
      recipient_id: employee_id,
      title: 'Deal Closed — Bonus Eligible',
      body: `Deal "${deal_name ?? 'N/A'}" worth ₹${(deal_value ?? 0).toLocaleString('en-IN')}` + ...`,
      type: 'info',
      created_at: new Date().toISOString(),
    })
  ).catch(() => {})
```

**Impact**

Ecosystem integrations (ICRM deal notifications) fail silently. Users don't receive commission/bonus alerts when deals close because the insert error is swallowed.

**Suggested fix**

supabaseAdmin.from('notifications').insert({...}).then(({ error }) => { if (error) console.error('[ecosystem] notif insert:', error); }).catch(e => console.error('[ecosystem] notif catch:', e)); or async IIFE without void.

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 26. Unvalidated employee_id POST parameter in leaves endpoint with cross-tenant check but no authorization on fire-and-forget email

- **File:** `app/api/leaves/route.ts:160`
- **Severity:** high
- **Dimension:** Route correctness
- **Category:** Fire-and-Forget Promise Without Error Handling

**What's wrong**

The email notification IIFE (lines 160–193) is not awaited and does not handle promise rejections. If the email send fails, the leave request is already persisted but the user is not notified. Additionally, the IIFE uses a bare function that could throw uncaught exceptions.

**Evidence**

```
;(async () => {
      try {
        const { data: emp } = await supabaseAdmin...
        const { data: hrs } = await supabaseAdmin...
        if (emp && hrs && hrs.length > 0) {
          const empName = `${emp.first_name} ${emp.last_name}`
          await Promise.all((hrs as any[]).map(hr =>
            hr.work_email ? sendLeaveSubmittedEmail({...}) : Promise.resolve()
          ))
        }
      } catch (e) { console.warn('[leaves POST] email notify non-fatal:', e) }
    })()
    return NextResponse.json({ data }, { status: 201 })
```

**Impact**

HR users don't receive leave request notifications even if the request is persisted. This breaks the approval workflow for leave requests.

**Suggested fix**

Move the email IIFE outside the try block or use .catch() and log errors; consider making it awaited with proper error escalation instead of silent catch.

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 27. Missing error check on bulk insert in announcements fanout

- **File:** `app/api/announcements/[id]/route.ts:50`
- **Severity:** high
- **Dimension:** Route correctness
- **Category:** Silently-Swallowed PostgREST Errors

**What's wrong**

The bulk insert of notifications is awaited but not destructured to check error. If the insert partially fails or fully fails due to FK constraint or quota, the error is swallowed.

**Evidence**

```
const notifications = (employees as Record<string, unknown>[]).map(emp => ({
      org_id: orgId,
      recipient_id: emp.id as string,
      title: notifTitle,
      body: snippet,
      type: notifType,
    }))
    await supabaseAdmin.from('notifications').insert(notifications)
```

**Impact**

Bulk notification inserts can fail silently, leaving some or all users without announcements. The fanOutNotification function returns success even on partial failure.

**Suggested fix**

const { error: bulkErr } = await supabaseAdmin.from('notifications').insert(notifications); if (bulkErr) { console.error('[announcements fanout]', bulkErr); throw bulkErr; }

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 28. Querying non-existent column 'grade' on designations table

- **File:** `app/api/designations/route.ts:25`
- **Severity:** high
- **Dimension:** Schema-code drift
- **Category:** Column Not Found

**What's wrong**

Code selects 'grade' column from designations table, but the schema only defines 'level' column. This will cause 500 errors at runtime.

**Evidence**

```
.select('id, title, grade, is_active, created_at')
```

**Impact**

GET /api/designations will fail with database error. All designation fetches broken.

**Suggested fix**

Replace 'grade' with 'level' or remove if unused; update INSERT/UPDATE/SELECT in all designations routes

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 29. Querying non-existent column 'hire_date' on employees table

- **File:** `lib/db/employees.ts:126`
- **Severity:** high
- **Dimension:** Schema-code drift
- **Category:** Column Not Found

**What's wrong**

Code selects 'hire_date' from employees, but schema defines 'date_of_joining'. This will cause runtime errors in employee queries.

**Evidence**

```
.select('status, department_id, employment_type, hire_date')
```

**Impact**

getHeadcountStats() and all code filtering on hire_date will fail. Dashboard and reporting broken.

**Suggested fix**

Replace 'hire_date' with 'date_of_joining' across all files (lib/db/*, app/api/*)

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 30. Referencing non-existent 'reporting_manager_id' column on employees

- **File:** `lib/db/dashboard.ts:57`
- **Severity:** high
- **Dimension:** Schema-code drift
- **Category:** Column Not Found

**What's wrong**

Code filters by 'reporting_manager_id' on employees, but schema only defines 'manager_id' (FK to employees(id)). Dashboard manager queries fail.

**Evidence**

```
.eq('reporting_manager_id', manager_id)
```

**Impact**

Manager dashboard queries fail. getManagerDashboardStats() and all manager-scoped reports broken.

**Suggested fix**

Replace 'reporting_manager_id' with 'manager_id' across lib/db/* and app/api/*

<sub>Verifier consensus: 2/3 real, confidence high</sub>

### 31. Querying non-existent 'period_start' and 'period_end' on performance_reviews

- **File:** `lib/db/performance.ts:26`
- **Severity:** high
- **Dimension:** Schema-code drift
- **Category:** Column Not Found

**What's wrong**

Code filters performance_reviews by 'period_start'/'period_end', but schema defines 'review_period_from'/'review_period_to'. Year filtering broken.

**Evidence**

```
.gte('period_start', `${filters.year}-01-01').lte('period_end', `${filters.year}-12-31`)
```

**Impact**

getPerformanceReviews() with year filter will fail. Performance cycle reports broken.

**Suggested fix**

Replace 'period_start'/'period_end' with 'review_period_from'/'review_period_to'

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 32. Selecting non-existent 'date' field on warning_letters (should be 'incident_date')

- **File:** `lib/db/warnings.ts:21`
- **Severity:** high
- **Dimension:** Schema-code drift
- **Category:** Column Not Found

**What's wrong**

Code tries to select and order by 'date' on warning_letters, but schema defines 'incident_date'. Also uses 'date' for year filtering at lines 27-28.

**Evidence**

```
.order('date', { ascending: false }); .gte('date', `${filters.year}-01-01`)
```

**Impact**

Warning letter queries fail. getWarnings() and getWarningStats() broken.

**Suggested fix**

Replace 'date' with 'incident_date' in all warning_letters queries

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 33. Accessing non-existent 'warning_type' column in warning stats

- **File:** `lib/db/warnings.ts:140`
- **Severity:** high
- **Dimension:** Schema-code drift
- **Category:** Column Not Found

**What's wrong**

getWarningStats() selects and filters by 'warning_type', but schema does not define this column. Warning categorization broken.

**Evidence**

```
.select('status, warning_type, employee_id, date')
```

**Impact**

Warning statistics endpoint fails; cannot filter warnings by type.

**Suggested fix**

Remove 'warning_type' or add column to schema; adjust filtering logic

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 34. Inserting non-existent columns into probation_reviews

- **File:** `app/api/employees/route.ts:263`
- **Severity:** high
- **Dimension:** Schema-code drift
- **Category:** Column Not Found

**What's wrong**

Employee creation tries to insert into probation_reviews with 'due_date' and 'outcome' columns, but schema does not define 'outcome' enum (only 'reviewed_at' or similar). INSERT will fail.

**Evidence**

```
INSERT { org_id: ctx.orgId, employee_id: data.id, due_date: ..., outcome: 'pending' }
```

**Impact**

New employee onboarding fails silently; probation reviews not created.

**Suggested fix**

Verify probation_reviews schema; remove 'outcome' if not real, or backfill with correct column

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 35. Selecting non-existent 'status' and 'is_wfh' on attendance_logs

- **File:** `lib/db/dashboard.ts:20`
- **Severity:** high
- **Dimension:** Schema-code drift
- **Category:** Column Not Found

**What's wrong**

Dashboard stats query selects 'status, is_wfh' and 'date' from attendance_logs, but schema defines attendance_logs as raw punch logs (log_date, punch_type, source). No status or is_wfh column.

**Evidence**

```
.select('status, is_wfh'); .eq('date', today)
```

**Impact**

HR dashboard fails to load attendance stats. Graphs and counts broken.

**Suggested fix**

Use attendance_daily table instead; or query attendance_logs and compute status in application

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 36. Filtering probation_reviews by non-existent columns 'due_date' and 'outcome'

- **File:** `lib/db/performance.ts:164`
- **Severity:** high
- **Dimension:** Schema-code drift
- **Category:** Column Not Found

**What's wrong**

getProbationReviews() tries to order by 'due_date' and filter by 'outcome', but schema only defines 'review_date' and 'outcome' (outcome IS real). due_date reference broken.

**Evidence**

```
.order('due_date', { ascending: true }); .lt('due_date', today).eq('outcome', 'pending')
```

**Impact**

Probation review dashboard fails; cannot identify overdue reviews.

**Suggested fix**

Use 'probation_end' or 'review_date' instead of 'due_date'

<sub>Verifier consensus: 3/3 real, confidence high</sub>

---

## Medium (20)

### 37. Payroll GET Endpoint Lacks Role Gating on Read Access

- **File:** `app/api/payroll/route.ts:23`
- **Severity:** medium
- **Dimension:** Auth + RLS boundary
- **Category:** Insufficient Role Gating

**What's wrong**

GET /api/payroll uses requireAuth() without role gating, exposing the list of all payroll runs in the org to any employee. Only POST is properly gated with requireRole(PAYROLL_ROLES).

**Evidence**

```
Line 23: const { ctx, error } = await requireAuth() in GET. Line 54: const { ctx, error } = await requireRole(PAYROLL_ROLES) in POST.
```

**Impact**

Any employee can enumerate payroll runs and retrieve their status, totals, and period information. Sensitive payroll metadata is visible to non-payroll staff.

**Suggested fix**

Add role check to GET: await requireRole(['owner', 'admin', 'hr_admin', 'super_admin', 'hr', 'payroll_admin'])

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 38. No state validation before approving leave request

- **File:** `c:/Users/ROSE MEHRA/Downloads/IHRMS-2/app/api/leaves/[id]/route.ts:143`
- **Severity:** medium
- **Dimension:** Business logic
- **Category:** State Transitions

**What's wrong**

approve action updates status to 'approved' without first checking current status is 'pending'. A non-pending leave can be re-approved, re-deducting leave balance twice.

**Evidence**

```
if (action === 'approve') {
  const { data, error: dbErr } = await supabaseAdmin
    .from('leave_requests')
    .update({ status: 'approved', approved_by: ctx.identityId, ... })
    .eq('id', id)
```

**Impact**

An already-approved leave request re-approved will trigger duplicate leave_balance decrement, making remaining_days negative. Payroll systems fail due to invalid balance state.

**Suggested fix**

Add where clause .eq('status', 'pending') before update, or check fetched row.status before proceeding

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 39. Manager can approve their own leave if they self-manage

- **File:** `c:/Users/ROSE MEHRA/Downloads/IHRMS-2/app/api/leaves/[id]/route.ts:117`
- **Severity:** medium
- **Dimension:** Business logic
- **Category:** Approval Flows

**What's wrong**

For non-HR roles, code checks if caller is the employee's reporting_manager_id. If an employee is their own reporting manager (edge case in seed data), they can approve their own leave.

**Evidence**

```
if (!FULL_ACCESS_ROLES.includes(ctx.role)) {
  const { data: emp } = await supabaseAdmin.from('employees').select('reporting_manager_id').eq('id', (leaveRow as any).employee_id)
  if (!emp || (emp as any).reporting_manager_id !== ctx.identityId) { return Forbidden }
}
```

**Impact**

Employees with no direct manager can bypass approval workflow. Unauthorized leave taken without oversight.

**Suggested fix**

Explicitly check leaveRow.employee_id !== ctx.identityId || reporting_manager_id !== ctx.identityId

<sub>Verifier consensus: 2/3 real, confidence high</sub>

### 40. PostgREST errors swallowed without logging in team-attendance GET

- **File:** `c:/Users/ROSE MEHRA/Downloads/IHRMS-2/app/api/team-attendance/route.ts:141`
- **Severity:** medium
- **Dimension:** Business logic
- **Category:** Error Handling

**What's wrong**

reportsErr from attendance_daily bulk query is logged but not thrown. If query fails (network, RLS denial), code continues with empty dailyRows, silently returning incomplete grid data instead of 500.

**Evidence**

```
const { data: dailyRows } = await supabaseAdmin.from('attendance_daily').select(...) as { data: AttendanceDailyRow[] | null }
// No check of error here; if PostgREST fails, dailyRows is implicitly undefined
```

**Impact**

Manager sees empty attendance grid and assumes no one worked that month. Absence of error alert means operational issues go unnoticed.

**Suggested fix**

Check and throw dailyRows error; return 500 instead of silently succeeding with no data

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 41. Attendance summary aggregation double-counts late status

- **File:** `c:/Users/ROSE MEHRA/Downloads/IHRMS-2/app/api/attendance/summary/route.ts:80`
- **Severity:** medium
- **Dimension:** Business logic
- **Category:** Business Logic

**What's wrong**

In the summary aggregation, status='late' increments both agg.present AND agg.late. This is intentional for attendance %, but unclear. If late is counted as a separate bucket elsewhere, totals will mismatch.

**Evidence**

```
if (isPresent) agg.present++
if (r.status === 'late') agg.late++
```

**Impact**

Attendance percentages and counts mismatch between different reports. Audit reconciliation fails.

**Suggested fix**

Document clearly or split 'present' into 'on_time' + 'late' to avoid ambiguity

<sub>Verifier consensus: 3/3 real, confidence high/medium</sub>

### 42. Payroll run status allows 'draft' → 'approved' but not intermediate states

- **File:** `c:/Users/ROSE MEHRA/Downloads/IHRMS-2/app/api/payroll/[id]/route.ts:89`
- **Severity:** medium
- **Dimension:** Business logic
- **Category:** State Transitions

**What's wrong**

approve action checks if status in ['processed', 'draft'], but code never transitions to 'processed'. This is incomplete state machine — expected states per schema are 'draft', 'processing', 'approved', 'paid'.

**Evidence**

```
if (!['processed', 'draft'].includes(current.status)) { return error }
updatePayload = { status: 'approved' }
```

**Impact**

Payroll never enters 'processing' or intermediate state, making workflow opaque. Admins can't track processing stage.

**Suggested fix**

Define full state machine; add POST action to mark payroll 'processing' after computation

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 43. Subscription cancellation state machine does not log who/why cancelled

- **File:** `c:/Users/ROSE MEHRA/Downloads/IHRMS-2/app/api/cron/billing/daily/route.ts:139`
- **Severity:** medium
- **Dimension:** Observability
- **Category:** Audit trail completeness

**What's wrong**

When a subscription transitions from trial → past_due → read_only → export_only → cancelled (via the daily cron), no audit log is created. There is no record in audit_logs or tenant_visible_audit of why the org was cancelled, who initiated it, or when each transition occurred.

**Evidence**

```
Line 139-145: Updates org_subscriptions.status and organisations.subscription_status with no accompanying logAudit() or tenant_visible_audit insert.
```

**Impact**

Customers and operators cannot see the reason for subscription cancellation. If a billing dispute arises, there is no audit trail showing that the cancellation was automatic due to non-payment, not an admin action.

**Suggested fix**

Insert a row into tenant_visible_audit for each subscription state transition (especially the final cancellation) with event_type 'imperial.subscription_cancelled' and reason.

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 44. Email send failures in mailer fire-and-forget with non-fatal catch

- **File:** `c:/Users/ROSE MEHRA/Downloads/IHRMS-2/lib/mailer.ts:216`
- **Severity:** medium
- **Dimension:** Observability
- **Category:** Silent failure in notification delivery

**What's wrong**

In sendAnnouncementEmails(), individual sends are wrapped in Promise.all().catch(() => {}). This swallows all errors from email delivery failures. If SMTP is down or misconfigured, the entire announcement send silently fails with no operator visibility.

**Evidence**

```
Line 216: `Promise.all(sends).catch(() => {})`
```

**Impact**

Announcement emails may fail to send to all or some recipients with no error logged. Operators have no way to know announcements were not delivered.

**Suggested fix**

Change to console.error('[mailer] sendAnnouncementEmails batch failed:', err) before swallowing; or return a partial-failure response indicating how many sends failed.

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 45. Impersonation token verification does not log signature failure

- **File:** `c:/Users/ROSE MEHRA/Downloads/IHRMS-2/app/api/auth/impersonation-login/route.ts:40`
- **Severity:** medium
- **Dimension:** Observability
- **Category:** Missing security event logging

**What's wrong**

When an impersonation token fails JWT verification (verifyImpersonationToken throws), the error is caught and a generic '401 Invalid or expired token' is returned. The underlying verification error is not logged, so token tampering, signature failures, or key rotation issues are invisible to operators.

**Evidence**

```
Line 40-43: `try { claims = await verifyImpersonationToken(token) } catch { return NextResponse.json({ error: 'Invalid or expired token' }, { status: 401 }) }` — the catch block swallows the error.
```

**Impact**

Security events (signature failures, key mismatches) are not logged. Operators cannot detect token tampering attempts or configuration problems.

**Suggested fix**

Log the underlying error: `catch (err) { console.error('[impersonation-login] token verification failed:', err); return NextResponse.json(...) }`

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 46. Billing subscription state transitions do not log to audit_logs

- **File:** `c:/Users/ROSE MEHRA/Downloads/IHRMS-2/app/api/cron/billing/daily/route.ts:74`
- **Severity:** medium
- **Dimension:** Observability
- **Category:** Audit trail for critical state changes

**What's wrong**

The daily billing cron transitions orgs through critical states (trial → past_due → read_only → export_only → cancelled) with no audit_logs entries. This is a cron action on behalf of the system, but it should still be audited as 'system' / 'billing_cron' actor.

**Evidence**

```
Line 74-81: Trial expiry triggers past_due status change with no logAudit() call.
```

**Impact**

The audit trail does not record who changed the subscription status or why. Customers and ops cannot see the billing state machine progression.

**Suggested fix**

After each state transition, call logAudit() with actor_identity_id='SYSTEM' or a reserved billing-cron identity, action='updated', module='subscription', summary describing the state change.

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 47. Employees route fetches optional columns in separate queries (inefficient multi-pass)

- **File:** `c/Users/ROSE MEHRA/Downloads/IHRMS-2/app/api/employees/route.ts:57`
- **Severity:** medium
- **Dimension:** Performance
- **Category:** N+1 Queries

**What's wrong**

The GET handler fetches the core employee list (line 31-39), then makes 3 additional separate queries (lines 62-84) to fetch optional columns (emp_id, role, is_admin, contact info, departments, designations). This is a 4-pass load instead of a single query.

**Evidence**

```
Lines 31-39: First query. Lines 61-68: Second query for ids_data. Lines 71-84: Third query for contact. Lines 88-105: Fourth query for departments (with retry). Lines 109-120: Fifth query for designations.
```

**Impact**

Moderate. For 50 employees, this incurs 5 database roundtrips instead of 1. On slow networks (typical in India), this could add 500ms+ latency.

**Suggested fix**

Consolidate into a single PostgREST query with all required columns and joins: .select('id, first_name, last_name, emp_id, work_email, ..., department:departments(...), designation:designations(...)').

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 48. Missing error check on org_branding insert in signup flow

- **File:** `app/api/auth/signup/complete/route.ts:116`
- **Severity:** medium
- **Dimension:** Route correctness
- **Category:** Promise Rejection in .then() Chain

**What's wrong**

The org_branding insert uses .then(({ error }) => ...) with only console.warn for logging. However, if the promise rejects (not PostgREST error but a network/parsing error), it's unhandled and could leave the signup in a partially-initialized state.

**Evidence**

```
await supabaseAdmin.from('org_branding').insert({ org_id: org.id } as never).then(({ error }) => {
      if (error) console.warn('[signup/complete] org_branding insert non-fatal:', error.message)
    })
```

**Impact**

A non-PostgREST error (e.g., network timeout during insert) will cause an unhandled promise rejection, potentially crashing the serverless function or leaving the org without branding configuration.

**Suggested fix**

Use .catch() or await and check error: const { error: brandErr } = await supabaseAdmin.from('org_branding').insert({ org_id: org.id }); if (brandErr) { console.warn('[signup/complete] org_branding:', brandErr); }

<sub>Verifier consensus: 2/3 real, confidence high/medium</sub>

### 49. Non-destructured Supabase query in team-attendance reports fetch

- **File:** `app/api/team-attendance/route.ts:141`
- **Severity:** medium
- **Dimension:** Route correctness
- **Category:** Missing Error Logging

**What's wrong**

The reports query checks for error but only logs it; it does not short-circuit or return an error response. The function proceeds to build grid rows even if the reports query failed, potentially causing undefined behavior downstream.

**Evidence**

```
if (reportsErr) {
      console.error('[team-attendance] reports query failed:', reportsErr.message)
    }
    const reportsList = (reports ?? []) as Array<{ ... }>
```

**Impact**

If the reports query fails (e.g., FK constraint mismatch on reporting_manager_id), the grid is built with empty data without informing the client of the error. The client receives an incomplete or misleading payload.

**Suggested fix**

if (reportsErr) { console.error('[team-attendance] reports query failed:', reportsErr.message); return NextResponse.json({ error: reportsErr.message }, { status: 500 }); }

<sub>Verifier consensus: 3/3 real, confidence high/medium</sub>

### 50. Missing error check on leave balance update in leaves [id] PATCH

- **File:** `app/api/leaves/[id]/route.ts:182`
- **Severity:** medium
- **Dimension:** Route correctness
- **Category:** Silently-Swallowed PostgREST Errors

**What's wrong**

The leave balance update query destructures result as `{ error: balUpdateErr }` and logs a warning, but does not re-throw. This leaves the leave_balances table out of sync with the approved leave request.

**Evidence**

```
const { error: balUpdateErr } = await supabaseAdmin
              .from('leave_balances')
              .update({ used_days: newUsed, remaining_days: newRemaining })
              .eq('id', bal.id)
            if (balUpdateErr) console.warn('[leaves approve] balance update error:', balUpdateErr)
```

**Impact**

Leave balances become inconsistent with approved leave. Over time, employees may exceed their leave limits because the used_days are not decremented, leading to audit issues and compliance violations.

**Suggested fix**

Either throw the error to rollback the leave approval, or return a 500 error if balance update fails. At minimum: if (balUpdateErr) { console.error('[leaves approve] balance update:', balUpdateErr); throw balUpdateErr; }

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 51. No error handling for auth.refreshSession() in auth/switch-org route

- **File:** `app/api/auth/switch-org/route.ts:108`
- **Severity:** medium
- **Dimension:** Route correctness
- **Category:** Incomplete Error Handling

**What's wrong**

The refreshSession() call explicitly doesn't throw (comment says non-fatal), but if it fails, subsequent API calls use stale JWT claims. The route still returns 200, even though the session refresh failed and client auth is misaligned.

**Evidence**

```
const { error: refreshErr } = await supabase.auth.refreshSession()
    if (refreshErr) {
      console.error('[auth/switch-org] refreshSession failed:', refreshErr)
      // Non-fatal — claims land on the next natural refresh.
    }
```

**Impact**

Client receives successful switch response but the JWT doesn't include the new org_id claim. All subsequent requests will fail with 403 Forbidden or return data from the old org, creating a confusing UX where the switch appears to work but API calls fail.

**Suggested fix**

Return the refreshErr to the client or wait for a manual refresh; don't silently continue with stale auth claims. Alternatively, ensure the response includes a flag telling the client to force a refresh.

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 52. Unhandled promise rejection in salary_structures insert in employees POST

- **File:** `app/api/employees/route.ts:250`
- **Severity:** medium
- **Dimension:** Route correctness
- **Category:** Fire-and-Forget Promise Without Error Handling

**What's wrong**

The salary_structures insert is awaited within a try-catch labeled 'non-critical', but if it throws an error other than PostgREST (e.g., JSON parsing), the error is swallowed and the employee creation is considered successful despite incomplete data.

**Evidence**

```
if (ctc_annual && data?.id) {
      const basic = Math.round(ctc_annual * 0.4 / 12)
      const hra   = Math.round(ctc_annual * 0.2 / 12)
      try {
        await supabaseAdmin.from('salary_structures').insert({
          org_id: ctx.orgId,
          employee_id: data.id,
          effective_from: hire_date,
          ctc_annual,
          basic_monthly: basic,
          hra_monthly: hra,
          special_allowance: Math.round(ctc_annual / 12) - basic - hra,
        })
      } catch { /* non-critical */ }
    }
```

**Impact**

An employee is created without a salary structure if the insert fails. Payroll processing will then fail or return incomplete data, breaking downstream payroll reports and calculations.

**Suggested fix**

Destructure the result and check { error } instead of catching; if critical, throw; if non-critical, log and document the fallback: const { error: salErr } = await supabaseAdmin.from('salary_structures').insert(...); if (salErr) console.warn('[employees POST] salary insert:', salErr);

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 53. Using non-existent 'review_type' filter on performance_reviews

- **File:** `lib/db/performance.ts:22`
- **Severity:** medium
- **Dimension:** Schema-code drift
- **Category:** Column Not Found

**What's wrong**

Code calls .eq('review_type') on performance_reviews, but schema has 'cycle' enum instead. Type mismatch.

**Evidence**

```
if (filters.review_type) query = query.eq('review_type', filters.review_type)
```

**Impact**

Performance review filtering by type fails; cycle-based filtering not exposed.

**Suggested fix**

Replace 'review_type' with 'cycle' or refactor filter logic

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 54. Referencing non-existent 'period_label' on payroll_runs

- **File:** `lib/db/dashboard.ts:22`
- **Severity:** medium
- **Dimension:** Schema-code drift
- **Category:** Column Not Found

**What's wrong**

Dashboard tries to select 'period_label' from payroll_runs, but schema only has month/year. Display logic broken.

**Evidence**

```
.select('status, period_label')
```

**Impact**

Payroll period display on dashboard fails; users see undefined instead of period label.

**Suggested fix**

Compute period label in application (e.g., 'April 2025' from month=4, year=2025)

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 55. Accessing undefined 'body.designation_title' in employee creation email

- **File:** `app/api/employees/route.ts:277`
- **Severity:** medium
- **Dimension:** Schema-code drift
- **Category:** Property Not Found

**What's wrong**

Employee welcome email is constructed with 'designation_title' from body, but the POST request body does not include this field (only designation_id). Email context will be incomplete.

**Evidence**

```
designation: body.designation_title,
```

**Impact**

Welcome email sent to new employees will have undefined designation; poor onboarding experience.

**Suggested fix**

Either accept designation_title in POST payload, or fetch designation details by designation_id before sending email

<sub>Verifier consensus: 2/3 real, confidence high</sub>

### 56. Selecting non-existent columns from appreciation_notes

- **File:** `lib/db/performance.ts:130`
- **Severity:** medium
- **Dimension:** Schema-code drift
- **Category:** Column Not Found

**What's wrong**

Code orders appreciation_notes by 'date', but schema defines 'created_at'. Ordering fails.

**Evidence**

```
.order('date', { ascending: false })
```

**Impact**

Appreciation notes list cannot be fetched; sorting fails.

**Suggested fix**

Replace 'date' with 'created_at'

<sub>Verifier consensus: 2/3 real, confidence high</sub>

---

## Low (9)

### 57. designation_title passed to sendWelcomeEmail but not from employee insert payload

- **File:** `c:/Users/ROSE MEHRA/Downloads/IHRMS-2/app/api/employees/route.ts:277`
- **Severity:** low
- **Dimension:** Business logic
- **Category:** Schema-Code Drift

**What's wrong**

sendWelcomeEmail called with body.designation_title, but POST body is never validated for this field. If omitted by client, email receives undefined.

**Evidence**

```
sendWelcomeEmail({ ..., designation: body.designation_title, department: body.department_name, ... })
```

**Impact**

Welcome email shows blank designation. Non-critical but unprofessional and hides missing data in client payload.

**Suggested fix**

Validate designation_title in request body or fetch from designations table by designation_id

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 58. Fire-and-forget email sends do not log failure details in sendInvitationEmail

- **File:** `c:/Users/ROSE MEHRA/Downloads/IHRMS-2/app/api/team/invite/route.ts:151`
- **Severity:** low
- **Dimension:** Observability
- **Category:** Missing error context in notifications

**What's wrong**

sendInvitationEmail() is called with .catch(() => {}), silently swallowing any SMTP errors. If the invitation email fails to send, there is no log entry, so the operator does not know the invitee never received their invitation.

**Evidence**

```
Line 151: `}).catch(() => {})`
```

**Impact**

Invitation emails may fail silently. Invitees will not receive their invitation link and the app returns success to the UI.

**Suggested fix**

Change to .catch(err => console.warn('[team/invite] sendInvitationEmail failed:', err)) or return the error to the caller.

<sub>Verifier consensus: 2/3 real, confidence high</sub>

### 59. Org welcome email silently fails without operator notification

- **File:** `c:/Users/ROSE MEHRA/Downloads/IHRMS-2/app/api/auth/signup/complete/route.ts:219`
- **Severity:** low
- **Dimension:** Observability
- **Category:** Missing error context in signup flow

**What's wrong**

sendOrgWelcomeEmail() in the signup flow is called with .catch(() => {}), so if SMTP is misconfigured or down, the customer never receives their welcome email but signup still succeeds.

**Evidence**

```
Line 225: `}).catch(() => {})`
```

**Impact**

New orgs do not receive welcome emails with no error indication. Customers will not know how to log in or what their trial period is.

**Suggested fix**

Log the error or queue it for retry: `.catch(err => console.warn('[signup/complete] org welcome email failed:', err))`

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 60. Dashboard stats uses 'exact' count on large tables

- **File:** `c/Users/ROSE MEHRA/Downloads/IHRMS-2/app/api/dashboard/stats/route.ts`
- **Severity:** low
- **Dimension:** Performance
- **Category:** Misuse of Count

**What's wrong**

The dashboard stats route does not explicitly use count, but the parallel queries fetch full result sets (employees, todayAttendance, pendingLeaves, etc.). While these use in-memory filtering, if any table grows to 100k+ rows, the query response time increases unnecessarily.

**Evidence**

```
Lines 25-29, 37-41, 62-66: Multiple .select('id') without count:'exact'. The in-memory filtering (lines 79-96) suggests the entire dataset is fetched.
```

**Impact**

Low impact currently, but dashboard load time may degrade with org growth. The queries are already optimized to select minimal columns.

**Suggested fix**

Consider using aggregation in PostgREST (SELECT COUNT(*) WHERE status='active') instead of fetching and filtering client-side.

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 61. Team-attendance route fetches org metadata and settings separately

- **File:** `c/Users/ROSE MEHRA/Downloads/IHRMS-2/app/api/team-attendance/route.ts:81`
- **Severity:** low
- **Dimension:** Performance
- **Category:** N+1 Queries

**What's wrong**

The route makes sequential queries: org info (line 81-85), tenant settings (line 88-92), leave types (line 106-110), manager lookup (line 121-126), reports (line 133-139), and attendance_daily (line 164-170). While each is necessary, the first 3 could be batched.

**Evidence**

```
Lines 81-110: Three sequential queries that don't depend on each other. These could use Promise.all().
```

**Impact**

Low. The route is already using some parallelism, but org metadata + settings + leave types could share a single batch.

**Suggested fix**

Wrap org/settings/leaveTypes queries in Promise.all() instead of sequential awaits.

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 62. Attendance daily summary route may exceed memory on large datasets

- **File:** `c/Users/ROSE MEHRA/Downloads/IHRMS-2/app/api/attendance/summary/route.ts:42`
- **Severity:** low
- **Dimension:** Performance
- **Category:** Unbounded Selects

**What's wrong**

The GET route fetches ALL attendance_daily records for a month (no limit) and aggregates them in memory (lines 66-89). For an org with 5000+ employees over 30 days, this is 150k+ rows loaded into Node.js memory.

**Evidence**

```
Lines 41-56: No .limit() on the attendance_daily query. All rows are fetched and then in-memory aggregation occurs.
```

**Impact**

Low for typical orgs, but could cause memory pressure on very large orgs. The aggregation logic is acceptable, but the unbounded fetch is risky.

**Suggested fix**

Consider adding a .limit(10000) as a safeguard, or implement streaming aggregation if org size exceeds threshold.

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 63. Missing error check on POST body parse in leaves endpoint

- **File:** `app/api/leaves/route.ts:85`
- **Severity:** low
- **Dimension:** Route correctness
- **Category:** Input Validation

**What's wrong**

req.json() is called without try-catch or error handling. If the request body is malformed JSON, the route handler will crash with a 500 error that includes a raw stack trace instead of a graceful error message.

**Evidence**

```
const body = await req.json()
    delete (body as Record<string, unknown>).org_id
    const { leave_type, start_date, end_date, from_date, to_date, days, reason, employee_id } = body
```

**Impact**

Malformed requests crash the handler instead of returning a clear 400 Bad Request error. Error messages leak implementation details.

**Suggested fix**

Wrap req.json() in try-catch or use a JSON schema validator (zod) to validate the entire body at once.

<sub>Verifier consensus: 3/3 real, confidence high</sub>

### 64. Audit logging not awaited in employees PATCH route

- **File:** `app/api/employees/[id]/route.ts:125`
- **Severity:** low
- **Dimension:** Route correctness
- **Category:** Fire-and-Forget Function Call

**What's wrong**

The emitEvent() and logAudit() calls are not awaited. If they fail, the error is not caught and the main response is returned. This is acceptable for audit-only operations but may cause subtle data loss if the function relies on audit side effects.

**Evidence**

```
// Ecosystem event + audit (fire-and-forget)
    emitEvent({...})
    logAudit({...})
```

**Impact**

Audit events may be silently dropped if the emitEvent or logAudit functions fail, leaving no trace of the employee update.

**Suggested fix**

.catch(() => {}) or await with proper error handling, or document this as intentional fire-and-forget.

<sub>Verifier consensus: 2/3 real, confidence high</sub>

### 65. Missing error handling for JSON parse in admin-users POST search

- **File:** `app/api/admin-users/route.ts:62`
- **Severity:** low
- **Dimension:** Route correctness
- **Category:** Input Validation

**What's wrong**

req.json() is called without error handling in the POST admin-users search/add action. If the JSON is malformed, the handler crashes.

**Evidence**

```
const body = await req.json() as { action?: string; q?: string; employee_id?: string; role?: string }
```

**Impact**

Malformed JSON requests crash the handler and return 500 with a stack trace instead of a graceful 400 Bad Request.

**Suggested fix**

Wrap in try-catch or use a schema validator. Alternatively, add .catch() to req.json().

<sub>Verifier consensus: 2/3 real, confidence high/medium</sub>
